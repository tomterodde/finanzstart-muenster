name: Daily Deploy

on:
  schedule:
    - cron: '0 7 * * *' # täglich 07:00 UTC = 09:00 MESZ
  workflow_dispatch: # manueller Trigger möglich

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel Rebuild
        run: curl -X POST "${{ secrets.VERCEL_DEPLOY_HOOK }}"
